# PRD — 프로젝트별 P&L 온라인 관리 서비스

작성: product-manager · 승인: ceo-advisor · 최종 수정: 2026-08-17

## 1. 배경 / 목적
기존에는 엑셀 관리대장(`TECH_STACK.md` 참고 스택 기준) + 로컬 HTML 대시보드로 프로젝트별 손익을 관리했다. 이를 온라인 서비스로 전환해 여러 회사가 각자의 계정으로 가입해 쓰는 **멀티테넌트 SaaS**로 만든다.

## 2. 대상 사용자
- 회사(조직) 단위 계정. 한 회사 안에 여러 명의 사용자가 role에 따라 접근.
- **하나의 이메일 계정은 동시에 하나의 회사에만 소속** (활성 멤버십 1개로 제한, DB 유니크 제약).
- 초기 목표: 소규모 팀/스타트업의 내부 손익 관리자.

## 3. 1차 범위 (MVP — 여기까지 한 번에 배포)
> 엑셀 업로드는 기존 데이터 마이그레이션에 필수이므로 1차에 포함한다.

1. 회사 생성 + 회원가입/로그인 (Supabase Auth)
2. 4단계 역할 기반 접근 제어, **role 단위 커스텀 권한 매트릭스** (아래 5절)
3. 프로젝트 목록 / 거래내역 CRUD (온라인 직접 입력)
4. 엑셀 업로드 → DB 저장 (기존 데이터 마이그레이션 + 이후 신규 데이터 반영), 업로드 데이터와 직접입력 데이터가 동일 테이블에서 합산되어 대시보드에 표시
5. **팀원 삭제 승인 워크플로우** (아래 5.3절)
6. 대시보드 (기존 `P&L 온라인 대시보드.html`의 KPI/차트/테이블 기능을 서버 데이터 기반으로 재구현)
7. **운영 설정 백오피스** (사업자정보/고객센터/DPO/탈퇴정책 — 아래 5.4절), 개인정보처리방침·이용약관 공개 페이지

## 4. 2차 범위 (MVP 이후)
- 대표계정 위임/승인 플로우

## 5. 권한 구조

### 5.1 역할 계층 (4단계, role 단위 커스텀 가능)
오너 → 관리자 → 팀 대표 → 팀원. 아래 표는 **회사 생성 시 기본값(default)**이며, **오너 또는 관리자가 회사별로 role 단위 권한을 재정의**할 수 있다 (개인별 오버라이드는 지원하지 않음 — 커스텀 단위는 role 전체).

| Role | 프로젝트/거래내역 | 사용자 초대 | 회사 설정 |
|---|---|---|---|
| 오너 | 전체 CRUD | 모든 하위 role 초대 | 가능 |
| 관리자 | 전체 CRUD | 팀대표/팀원 초대 | 일부 |
| 팀 대표 | 소속 팀 CRUD | 팀원 초대 | 불가 |
| 팀원 | 소속 팀 조회 + 입력 + 삭제 요청(승인제, 5.3절) | 불가 | 불가 |

- 권한 매트릭스 수정 화면: 오너/관리자만 접근. 수정 시 해당 회사에만 적용(다른 회사 영향 없음).

### 5.2 대표계정 (별도 플래그, role과 독립)
- 회사당 정확히 1명. role 단계와 무관하게 부여 가능 (예: 팀원도 대표계정일 수 있음).
- 실질 권한 보유: 회사 삭제, 결제/구독 관리 등 대표계정만 가능한 액션 별도 존재.
- **위임 플로우 (2차)**: 현 대표가 위임 요청 → 대상자 승인 → 대표계정 이관 → 기존 대표는 본인이 새 role 직접 선택.

### 5.3 팀원 삭제 승인 워크플로우
- **대상**: 팀원(role)이 프로젝트/거래내역을 삭제하려는 경우에만 적용. 팀 대표 이상은 즉시 삭제.
- **승인자**: 요청자가 속한 팀의 팀 대표. 팀 대표가 지정되어 있지 않으면 관리자에게 자동 에스컬레이션.
- **플로우**: 팀원이 삭제 요청 → 상태 `대기중`으로 기록(실제 삭제는 보류) → 팀 대표(또는 관리자)에게 알림 → 승인 시 실제 삭제 실행 / 반려 시 요청 종료 및 사유 기록.
- 이 플로우가 커스텀 권한 매트릭스에서 "팀원 삭제 불가"로 재정의된 회사에서는 삭제 요청 자체가 비활성화된다.

### 5.4 플랫폼 운영자 (회사 계층 밖 별도 액터)
- 4단계 role과 무관한 **서비스 운영팀 권한**. 회사 소속이 아니라 플랫폼 전체에 대한 접근.
- **역할은 "기존 멤버십 해제"까지만**. 새 회사 멤버십 생성은 운영자가 대신 해주지 않고, 해제된 사용자가 **본인이 직접** 아래 둘 중 하나를 셀프서비스로 진행한다 (둘 다 이미 존재하는 일반 플로우, 별도 화면 불필요):
  - (a) 다른 회사의 초대 링크를 수락 (이직)
  - (b) 본인이 새 회사를 생성해 오너로 가입 (창업/독립 사업 시작)
- **요청 접수 (1차)**: 앱 내 UI 없이 고객센터 채널(이메일 등)로 해제 요청 접수 → 운영자가 Supabase 콘솔에서 기존 멤버십 비활성화 → 사용자에게 완료 안내 → 사용자가 (a) 또는 (b) 진행.
- DB 제약("이메일당 활성 멤버십 1개")은 1차 스키마에 반드시 포함.
- 회사 이관용 정식 백오피스 UI는 2차 이후 검토 (변동 없음).

**1차에 포함되는 예외 — 운영 설정 백오피스**: 사업자등록정보/고객센터 연락처/개인정보 보호책임자/회사 탈퇴 시 데이터 정책 문구는 법적 문서(개인정보처리방침 등)에 노출되는 값이라 배포 없이 수정 가능해야 한다. `platform_settings` 테이블 + 운영자 전용 설정 화면을 1차에 포함한다 (회원 이관 기능과 달리 민감도가 낮은 텍스트 편집 수준이므로). **누가 운영자(`platform_admins`)가 되는지는 여전히 Supabase 콘솔에서 수동 등록** — 자기 자신을 운영자로 임명하는 셀프서비스 경로는 만들지 않는다.

### 5.5 ADMIN 백오피스 전체 범위 (2026-08-17 확정, 상세는 `ARCHITECTURE.md` 참고)
운영자(플랫폼 관리자)용 백오피스는 아래 4개 기능으로 구성되고, 기능별로 통제 수준이 다르다.

| 기능 | 범위 | 통제 |
|---|---|---|
| 운영 설정 | 5.4절과 동일 | 쓰기: 운영자만, 읽기: 공개 |
| **회원관리** | **계정 정보 + 소속 회사의 실제 PNL 데이터(프로젝트/거래내역) 열람 전용** (수정·삭제 불가) — "회사간 데이터 절대 격리" 원칙의 의도적 예외 | 반드시 감사 로그(`platform_admin_access_log`)를 남기는 RPC 경유로만 열람. RLS로 조용히 열어주는 경로는 만들지 않음 |
| PNL 데이터 현황 | 회사별 사용 현황 **메타데이터만**(멤버 수/프로젝트 수/거래 건수) — 실제 금액·항목명은 비노출 | 집계 조회, 로그 불필요 (개별 회사 상세 열람이 아니므로) |
| 공지사항 / FAQ | 공개 콘텐츠 관리, **공지사항은 PNL USER 로그인 후 대시보드에도 노출** | 일반 RLS(쓰기: 운영자만, 읽기: 공개) |

**운영자 등급 (2026-08-17 확정)**: `platform_admins`는 `super_admin`(최고 관리자)과 `operator`(일반 운영자)로 나뉜다. **감사 로그는 super_admin이거나, 개별로 `can_view_audit_log` 권한을 부여받은 operator만 열람 가능** — 일반 운영자는 기본적으로 감사 로그를 볼 수 없다. 등급/권한 부여는 `platform_admins` 등록과 마찬가지로 앱 UI가 아니라 Supabase 콘솔에서만 처리한다(일관된 신뢰 경계).

## 6. 데이터 모델 개요 (backend-developer 상세 설계 전 가이드)
- `companies` — 테넌트 단위
- `users` — Supabase Auth 사용자
- `memberships` — user × company × role(4단계) × is_representative(bool) × team(팀 대표/팀원 소속 팀 구분용)
- `role_permissions` — company_id × role × 권한 항목(CRUD/초대/설정 등 boolean) — 회사별 커스텀 오버라이드 저장, 없으면 기본값 적용
- `deletion_requests` — 요청자, 대상 레코드(project/transaction), 승인자(팀 대표 or 관리자), 상태(대기중/승인/반려), 사유
- `projects`, `transactions` — 기존 엑셀 시트(프로젝트목록/거래내역) 구조 계승, `company_id` FK로 격리
- `platform_admins` — 회사 소속과 무관한 플랫폼 운영자 allowlist (Supabase 콘솔에서만 등록/제거)
- `platform_settings` — 사업자정보/고객센터/DPO/탈퇴정책 싱글턴 설정, 공개 읽기 + 운영자 전용 쓰기
- `platform_admin_access_log` — 운영자가 회원관리에서 특정 회사의 실제 데이터를 열람할 때마다 자동 기록 (5.5절)
- `notices`, `faqs` — 공지사항/FAQ, 공개 읽기 + 운영자 전용 쓰기
- 모든 회사 데이터 테이블 RLS: `company_id` 기준 격리 필수 (다른 회사 데이터 절대 접근 불가). `platform_settings`/`notices`/`faqs`는 예외적으로 읽기가 전체 공개(비로그인 포함). 회원관리의 회사별 실데이터 열람은 RLS가 아니라 감사 로그를 남기는 RPC로만 허용(5.5절)

## 7. 비기능 요구사항 / 플랫폼 로드맵
- **반응형 웹 (1차 필수)**: 데스크톱 + 모바일 브라우저 모두 지원. Tailwind CSS 기반으로 breakpoint 대응 (`TECH_STACK.md` 스택 유지).
- **하이브리드 앱 (2차 이후 로드맵)**: 향후 Flutter 기반 iOS/Android 앱 확장을 염두에 둔다. 이에 따라 1차부터 **API-first 설계**를 원칙으로 한다 — 웹 프론트엔드도 Supabase 클라이언트 SDK/REST를 통해 백엔드와 통신하고, 웹 전용 가정(세션 쿠키 전용 인증 등)에 종속되지 않게 구현한다. (Supabase는 Flutter 공식 SDK를 지원해 추후 확장 시 백엔드 재설계 부담이 적음)

## 8. 보안 요구사항
- Supabase RLS로 회사 간 데이터 완전 격리 (설계 단계부터 privacy-security-officer 검토)
- 재무 데이터 + 계정 정보 다루므로 인증/인가 로직은 QA 리뷰 필수, 배포 전 qa-reviewer 승인 필요
- 엑셀 업로드 파일 검증(형식, 크기, 악성 콘텐츠) 서버 측에서 처리

## 9. 비범위 (1차에서 제외)
- 대표계정 위임/승인 플로우
- 개인(사용자)별 권한 오버라이드 (커스텀 단위는 role까지만)
- 다중 통화 환율 계산 (기존 템플릿엔 통화 필드만 존재)
- 플랫폼 운영자 정식 백오피스 UI (1차는 Supabase 콘솔 수동 처리)
- Flutter 하이브리드 앱 (로드맵만 반영, 구현은 별도 프로젝트로 진행)

## 10. 다음 단계
1. service-planner — 화면별 상세 플로우/화면정의서 (가입, 초대, 입력, 업로드, 대시보드) — 진행 중
2. backend-developer — Supabase 스키마 + RLS 정책 확정
3. privacy-security-officer — RLS/Auth 설계 리뷰
4. frontend-developer — 화면 구현 (반응형)
5. qa-reviewer — 배포 전 리뷰
