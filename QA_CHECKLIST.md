# 수동 QA 체크리스트

작성: 2026-08-17. 코드 레벨 자동 검증(타입체크/lint/빌드)은 이미 통과된 상태 — 여기는 실제 브라우저 클릭 테스트용.

## 0. 시작 전 — 반영 필요한 SQL (순서대로)
SQL Editor에서 아래 파일들을 **번호 순서대로** 실행 (0001은 이미 적용됨):
- [ ] `0002_excel_upload_permission_fix.sql`
- [ ] `0003_profiles_email.sql`
- [ ] `0004_memberships_profiles_fk.sql` — QA 중 발견한 버그 수정 (팀원 관리 화면 조인 실패)
- [ ] `0005_team_scoping_null_fix.sql` — QA 중 발견한 버그 수정 (팀 대표 권한 스코핑 누락)

## 1. 계정 준비
- [ ] 오너 계정 1개 (이미 가입 완료하신 계정)
- [ ] `platform_admins` 테이블에 오너 계정의 `user_id`로 row 추가 (`role='super_admin'`, `can_view_audit_log=true`) — ADMIN 테스트용
- [ ] 팀원 관리 화면에서 관리자 1명, 팀 대표 1명, 팀원 1명 초대 (각각 다른 이메일 필요 — 실제 받을 수 있는 이메일 or `Confirm email` 꺼두고 테스트)

## 2. 인증 플로우
- [ ] `/` 접속 시 비로그인이면 `/login`으로, 로그인 상태면 `/dashboard`로 리다이렉트
- [ ] 잘못된 비밀번호로 로그인 → 에러 메시지
- [ ] 초대 이메일과 다른 계정으로 로그인한 상태에서 초대 링크 접속 → "이 초대는 OO 앞으로 온 것입니다" 안내 + 로그아웃 유도
- [ ] 이미 다른 회사 소속인 이메일로 신규 회사 가입 시도 → 차단 메시지

## 3. 프로젝트/거래내역 CRUD — **오너로 테스트**
- [ ] 프로젝트 등록 → 목록에 표시
- [ ] 프로젝트 수정 (인라인) → 저장 후 반영
- [ ] 거래내역 등록 (수익/비용 각각) → 금액 0 이하 입력 시 에러
- [ ] 거래내역 수정
- [ ] 프로젝트/거래내역 **즉시 삭제** (확인 모달 → 삭제)

## 4. 삭제 승인 워크플로우 — **팀원으로 테스트**
- [ ] 팀원 계정으로 로그인 → 거래내역 옆에 "삭제" 대신 "삭제 요청" 버튼만 보이는지
- [ ] 삭제 요청 시 사유 입력 필수 확인
- [ ] 요청 후 해당 행에 "삭제 승인 대기중" 표시로 바뀌고 수정/재요청 버튼이 사라지는지
- [ ] 팀원 계정에는 "삭제 승인함", "팀원 관리" 메뉴가 안 보이는지
- [ ] **팀 대표 또는 관리자 계정으로 전환** → `/deletion-requests`에서 요청 확인 → 승인 → 실제로 삭제되는지
- [ ] 다른 요청을 만들고 이번엔 **반려** → 사유 입력 후 반려, 팀원 쪽에서 대기 상태가 풀리는지(재요청 가능해지는지)
- [ ] (가능하면) 팀원 본인이 요청한 건을 같은 팀원 계정으로 승인 시도 → 애초에 승인 버튼 자체가 안 보임(정상)

## 5. 권한 매트릭스 — **오너로 테스트**
- [ ] `/settings/permissions`에서 "팀원" 카드의 `transaction_create` 체크 해제 → 저장
- [ ] 팀원 계정으로 재로그인 → 거래내역 입력 버튼이 사라졌는지 확인
- [ ] "기본값 복원" 클릭 → 원래대로 돌아오는지

## 6. 팀/초대 — **오너·관리자·팀 대표 각각 테스트**
- [ ] 오너가 관리자 초대 → 정상 발송
- [ ] 관리자가 오너로 초대 시도 → 선택지에 아예 없어야 함
- [ ] **팀 대표로 로그인** → 초대 화면에서 "팀" 필드가 본인 팀으로 고정되어 있고 수정 불가한지 (QA 중 고친 부분)
- [ ] 팀 대표가 팀원 초대 → 정상 발송, 관리자 이상 역할은 선택지에 없는지
- [ ] 초대중 목록에서 재발송/취소 동작 확인
- [ ] 관리자가 팀원의 role을 "팀 대표"로 변경 → 반영 확인
- [ ] 회사에 오너가 1명뿐일 때 그 오너를 비활성화 시도 → 차단 메시지("최소 1명의 오너 필요")

## 7. 엑셀 업로드 — **오너로 테스트**
- [ ] `프로젝트별_PL_관리대장_2026.xlsx` 원본 템플릿(예시 행 있는 상태)으로 업로드 → 예시 행은 파싱에서 자동 제외되는지
- [ ] 실제 데이터로 채운 파일 업로드 → 프리뷰에서 신규 프로젝트/신규 거래 배지 확인
- [ ] 같은 파일 재업로드 → 이번엔 "중복 의심" 배지 + 기본 체크 해제 상태로 뜨는지
- [ ] 일부만 체크하고 저장 → 결과 화면의 저장/제외 건수가 맞는지
- [ ] 저장된 데이터가 프로젝트 상세의 거래내역에 "엑셀 업로드" 출처로 표시되는지

## 8. 대시보드 — **오너로 테스트**
- [ ] 데이터 입력 후 KPI 6종 숫자가 실제 합계와 맞는지 (총매출/총비용/순이익/이익률/진행중/진행완료)
- [ ] 연도 필터 변경 시 KPI/막대/파이는 바뀌는데, 추이선 차트는 안 바뀌는지 (의도된 동작)
- [ ] 상태/검색 필터는 추이선에도 반영되는지
- [ ] 프로젝트별 순이익 막대 — 적자 프로젝트가 빨간색으로 나오는지
- [ ] 테이블 컬럼(날짜/프로젝트/금액) 클릭 시 정렬 방향이 바뀌는지
- [ ] 공지사항을 ADMIN에서 게시하면 대시보드 상단에 뜨는지 (8번 항목과 연계)

## 9. ADMIN 백오피스 — **super_admin으로 테스트**
- [ ] `platform_admins`에 없는 일반 계정으로 `/admin/*` 접속 시 `/dashboard`로 리다이렉트되는지
- [ ] `/admin/settings`에서 사업자정보 저장 → `/privacy` 공개 페이지에 즉시 반영되는지
- [ ] `/admin/companies` 목록에서 실제 금액이 안 보이고 건수만 보이는지
- [ ] 회사명 클릭 → 상세에서 멤버/프로젝트/거래내역 탭 전환, **수정 버튼이 없는지**(열람 전용)
- [ ] `/admin/audit-log`에서 방금 전 상세 열람 기록이 남았는지 (탭 전환할 때마다 각각 기록되는지)
- [ ] `platform_admins`에서 본인을 `role='operator'`, `can_view_audit_log=false`로 바꾸고 재로그인 → 감사 로그 메뉴/페이지 접근 차단되는지, 회원관리는 여전히 되는지
- [ ] 공지사항/FAQ 작성 → 게시 여부 토글 → PNL USER 쪽에 비공개 항목은 안 보이는지

## 10. 알려진 한계 (버그 아님, 참고용)
- 팀 대표가 **다른 팀** 소속 프로젝트/거래내역에 대해 "삭제" 버튼을 누르면(UI는 버튼을 보여줌) 서버가 권한 오류로 막습니다 — 데이터는 안전하지만 에러 메시지로 처리되는 방식입니다. 팀별로 화면을 아예 분리하는 건 2차 개선 과제로 남겨뒀습니다.
- 팀 없는(회사 전체 범위) 프로젝트는 팀 대표/팀원이 만들 수 없고 오너/관리자만 만들 수 있습니다 (QA 중 확정한 동작).
